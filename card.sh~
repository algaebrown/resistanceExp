# list of genome
# hermuba/data/allgenomelist.txt

# list of done card annotation
# 

# first need to convert to python 2.7
source actiave py27

# then generation of list needed
cd hermuba/data/genePredicted/txt
ls -A1 | grep .faa | sed -e 's/\.fna//g' > oldList

# difference
diff 
